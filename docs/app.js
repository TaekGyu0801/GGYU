async function loadStatus(){const r=await fetch("data/status.json",{cache:"no-store"});if(!r.ok)throw new Error("status.json load failed");return r.json();}
function el(tag,cls,text){const n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=text;return n;}
loadStatus().then(data=>{document.querySelector("#current-stage").textContent=data.currentStage;document.querySelector("#goal").textContent=data.goal;document.querySelector("#blocker").textContent=data.currentBlocker;document.querySelector("#updated").textContent="Last synchronized: "+data.updated;const phases=document.querySelector("#phases");data.phases.forEach(p=>{const card=el("article","phase");const badge=el("span","status "+p.status,p.status);card.appendChild(badge);card.appendChild(el("h3","","Phase "+p.id+" — "+p.title));const bar=el("div","bar");const fill=el("span");fill.style.width=(p.progress||0)+"%";bar.appendChild(fill);card.appendChild(bar);card.appendChild(el("div","",(p.progress||0)+"%"));const a=el("a","","Issue #"+p.issue+" →");a.href="https://github.com/TaekGyu0801/GGYU/issues/"+p.issue;card.appendChild(a);phases.appendChild(card);});const members=document.querySelector("#members");data.members.forEach(m=>{const card=el("article","member");card.appendChild(el("div","eyebrow","Member"));card.appendChild(el("h3","",m.name));card.appendChild(el("p","",m.role));const a=el("a","","Workspace →");a.href="https://github.com/TaekGyu0801/GGYU/tree/main/"+m.folder;card.appendChild(a);members.appendChild(card);});[["#confirmed","confirmed"],["#unresolved","unresolved"]].forEach(pair=>{const ul=document.querySelector(pair[0]);data[pair[1]].forEach(x=>ul.appendChild(el("li","",x)));});}).catch(err=>{document.querySelector("#current-stage").textContent="Dashboard data load error";document.querySelector("#blocker").textContent=err.message;});